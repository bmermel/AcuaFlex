/// Anchos de referencia para layout responsive (móvil).
abstract class AppBreakpoints {
  AppBreakpoints._();

  /// Por debajo de esto, las filas de botones típicas se apilan en columna.
  static const double compactButtonRow = 420;

  /// Padding horizontal reducido (pantallas muy angostas).
  static const double narrowScreenWidth = 360;
}
